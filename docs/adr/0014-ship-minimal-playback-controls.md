# Ship minimal playback controls

Mojify v1 includes only quit and pause/resume controls during playback. This makes the player usable enough to inspect and stop cleanly while avoiding seeking, speed control, and zoom until the scheduler and renderer are stable.
