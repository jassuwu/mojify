# Use a monorepo with a Go-first product

Mojify starts as a monorepo so the site, docs, future npm package, assets, and release tooling have clear homes from the beginning. The first runnable product is still the Go core CLI; TypeScript infrastructure should support the project shape without becoming a required wrapper for v1 playback.
