# Defer audio and export from v1 playback

Mojify v1 defines success as smooth visual terminal playback of local video files. Audio sync and traditional media exports are intentionally deferred because they introduce separate scheduling, font rendering, encoding, and distribution problems before the core terminal renderer has been proven.
