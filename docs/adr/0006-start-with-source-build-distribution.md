# Start with source-build distribution

Mojify v1 begins as a source-build project rather than an npm/npx package or prebuilt native binary. This keeps the first phase focused on the renderer, playback pipeline, and internal architecture before taking on cross-platform binary packaging, npm optional packages, checksums, and release automation.
