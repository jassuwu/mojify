# Defer zoom controls

Mojify v1 does not include interactive or configured zoom controls. Zoom would require terminal raw input, crop state, and scheduler-safe interaction handling before the basic terminal playback experience has been proven.
