# Use Go for the v1 core

Mojify v1 uses Go for the native core. Go is fast enough for the first renderer/player, readable for the project owner, and simpler to build and distribute than C/C++; Rust remains a possible future option if packaging or performance needs demand it.
