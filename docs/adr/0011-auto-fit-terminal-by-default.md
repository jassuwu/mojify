# Auto-fit terminal size by default

Mojify v1 chooses a render grid that fits the current terminal by default, with an explicit width override available later for repeatable demos. A terminal video player should produce a usable frame when run without tuning, and `probe` can expose the derived dimensions when users need to understand or debug sizing.
