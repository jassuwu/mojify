# Use golden renderer tests first

Mojify's first automated quality anchor is golden renderer tests over small input frames. The renderer recipe is the project's differentiator, so tests should lock character density, color, and edge-glyph behavior before broader CLI and playback smoke tests are added.
